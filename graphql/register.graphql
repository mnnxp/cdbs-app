query RegisterOpt {
  programs {
    id
    name
  }

  regions {
    regionId
    langId
    region
  }

  typesAccess {
    typeAccessId
    langId
    name
  }
}

mutation RegUser($iptUserData: IptUserData!) {
  registerUser(args: $iptUserData) {
    uuid
    username
    programId
  }
}
