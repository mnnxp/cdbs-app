query RegisterOpt {
  programs {
    id
    name
  }

  typesAccess(typeAccessIds: [1,3]) {
    typeAccessId
    langId
    name
  }
}

mutation RegUser($iptUserData: IptUserData!) {
  registerUser(args: $iptUserData) {
    uuid
    username
    programId
  }
}
