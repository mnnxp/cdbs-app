query RegisterOpt {
  programs {
    id
    name
  }

  regions {
    regionId
    langId
    region
  }
}

mutation RegUser($iptUserData: IptUserData!) {
  registerUser(args: $iptUserData) {
    uuid
    username
    programId
  }
}
